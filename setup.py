from setuptools import setup, find_packages

setup(
    name='Game',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
    tests_require=[
        'pytest',
    ],
)
